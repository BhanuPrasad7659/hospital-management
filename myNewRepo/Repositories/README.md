# Repositories

This folder contains the repository interfaces and implementations for database access.
- **Interfaces**: Define the data access contracts.
- **Implementations**: Implement the interfaces using Entity Framework Core (`HospitalDbContext`).
